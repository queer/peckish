**NOTE:** The OCI artifact/producer are WIP.

# artifact

```yaml
name: "my oci artifact"
type: "oci"
path: "./path-to-artifact.tar"
```

# producer

```yaml
name: "my oci artifact producer"
path: "./path-to-output-artifact.tar"
architecture: "amd64"
```
